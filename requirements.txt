# path: requirements.txt
# Shared by Vercel serverless functions (api/*.py, api_server.py) AND the
# Railway worker (discord_bot/bot.py) — see requirements-bot.txt for the
# Discord/media-only packages, kept OUT of this file so Vercel doesn't
# bundle them into every function (Vercel only ever reads requirements.txt).

aiohttp>=3.8.0
httpx>=0.24.0
asyncpg>=0.29.0
python-dotenv>=0.19.0
requests>=2.28.0
cryptography>=42.0.0

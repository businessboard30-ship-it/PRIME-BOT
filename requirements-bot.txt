# path: requirements-bot.txt
# Discord bot / media-processing deps — only needed by discord_bot/bot.py
# (the Railway "worker" process). Installed on Railway via nixpacks.toml's
# install phase override. Vercel never reads this file, so these packages
# never get bundled into the /api serverless functions.

discord.py>=2.6.0
pytest-asyncio>=0.23.0
yfinance>=0.2.32
yt-dlp>=2026.7.4
PyNaCl>=1.5.0
davey>=0.1.5
groq>=0.4.2
Pillow>=10.0.0
